# projectgulp
[Node js Version: v13.6.0]

# Installation
  OS X & Linux:
  npm i gulp (in the project folder)
  gulp build:dev  (terminal)
  gulp server (terminal)
  
  Windows:
  npm i gulp (in the project folder)
  gulp build:dev  (terminal)
  gulp server (terminal)


